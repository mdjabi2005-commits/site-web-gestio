[pytest]
# Dossier racine des tests
testpaths = tests

# Affichage détaillé par défaut
addopts = -v --tb=short

# Marqueurs personnalisés
markers =
    unit: Tests unitaires purs (sans DB ni fichiers)
    integration: Tests avec base de données
    ocr: Tests liés au module OCR

